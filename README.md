Shubham Singh — Single-page portfolio

Open `index.html` in your browser to view the portfolio locally.

Files:
- index.html
- styles.css
- script.js
- assets/images/*.svg

To view locally on Windows, double-click `index.html` or run:

```powershell
Start-Process index.html
```
